[build]
  publish = "."
  functions = "netlify/functions"
  command = "mkdir -p netlify/functions && cp index.html netlify/functions/index.html && echo 'Copied index.html to functions'"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/app"
  status = 200
  force = true